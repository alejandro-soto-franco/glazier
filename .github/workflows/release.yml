# PyPI publish workflow for the `glazier` Python package (glazier-py crate).
#
# Configured under the PyPI Trusted Publisher for project `glazier`:
#   repository: alejandro-soto-franco/glazier
#   workflow:   release.yml
#   environment: pypi
#
# The wheel is pure Rust (the CUDA `cuda` feature is opt-in and NOT built here),
# so wheels build directly on the GitHub runners with maturin: no CUDA toolchain,
# no local build step. abi3-py310 means one wheel per platform covers Python 3.10+.
#
# Trigger: push a tag like `v0.1.0`, or run manually. Publishing uses OIDC
# trusted publishing, so there is no PyPI token in repository secrets.

name: release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  wheels:
    name: wheels (${{ matrix.platform.os }} / ${{ matrix.platform.target }})
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          # The x86_64 Linux wheel is built with the GPU feature: cudarc uses
          # dynamic-loading, so no CUDA toolchain is needed at build time and the
          # wheel stays manylinux-portable (it dlopens the driver at runtime, and
          # a run on a machine with no device reports it). Other platforms
          # ship CPU-only; the GPU functions still exist and raise a clear error.
          - { os: linux,   runner: ubuntu-latest,  target: x86_64,  manylinux: auto, features: "--features cuda" }
          - { os: linux,   runner: ubuntu-latest,  target: aarch64, manylinux: auto, features: "" }
          # Both macOS wheels build on Apple Silicon: the x86_64 wheel is
          # cross-compiled (Xcode targets both arches natively), so we never
          # request a scarce, being-retired macos-13 Intel runner.
          - { os: macos,   runner: macos-latest,   target: x86_64,  manylinux: "", features: "" }
          - { os: macos,   runner: macos-latest,   target: aarch64, manylinux: "", features: "" }
          - { os: windows, runner: windows-latest, target: x64,     manylinux: "", features: "" }
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.x"
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          manylinux: ${{ matrix.platform.manylinux }}
          args: --release --out dist --manifest-path glazier-py/Cargo.toml ${{ matrix.platform.features }}
          sccache: "true"
      - uses: actions/upload-artifact@v5
        with:
          name: wheels-${{ matrix.platform.os }}-${{ matrix.platform.target }}
          path: dist

  publish:
    name: Publish to PyPI
    needs: [wheels]
    runs-on: ubuntu-latest
    environment:
      name: pypi
    permissions:
      id-token: write # required by PyPI trusted publishing (OIDC)
    steps:
      - uses: actions/download-artifact@v5
        with:
          pattern: wheels-*
          merge-multiple: true
          path: dist
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist
